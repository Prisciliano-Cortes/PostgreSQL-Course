-- VIEW IN THIS LESSON
-- 1. MASSIVE UPDATES
-- 2. INFORMATION CREATION AND DUMP
-- 3. ALTERATION OF INDICES AND CHECKS
-- 4. ELIMINATION OF CHECKS
-- 5. MODIFICATION OF COLUMNS USING GUI AND MANUALLY
-- 6. TABLE CREATION
-- 7. RELATIONS

SELECT continent FROM country

SELECT DISTINCT continent FROM country ORDER BY continent ASC

CREATE IF NOT EXIST TABLE "public"."continent"(
    "code" int4 NOT NULL,
    "name" TEXT NOT NULL,
    PRIMARY KEY("code")
)

-- INSERT DATA IN TABLE CONTINENT USING SELECT OF THE OTHER TABLE
INSERT INTO continent (name) SELECT DISTINCT continent FROM country ORDER BY continent ASC

SELECT * FROM continent